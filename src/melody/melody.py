import typer
from typing_extensions import Annotated
import subprocess

def melody():
    print("m1")
    # action = arg[0]
    # print("You have selected - ",action)
    # if action.strip() == "run":
    #     print("run -todo")
    #     res = subprocess.run(["flwr", "run","../flower_text/quickstart-pytorch"])
    #     print(res.stdout)
    # elif(action.strip() == "log"):
    #     print("log - todo")
    # elif(action.strip() == "stop"):
    #     print("stop - todo")
    # elif(action.strip() == "pull"):
    #     print("pull - todo")
    # else:
    #     print("Unknown action - "+action)
    # greeting = "Greetings, "
    # if doctor and not title:
    #     title = "Dr."
    # if not name:
    #     if title:
    #         name = title.lower().rstrip(".")
    #     else:
    #         name = "friend"
    # if title:
    #     greeting += f"{title} "
    # greeting += f"{name}!"
    # for i in range(0, count):
    #     print(greeting)